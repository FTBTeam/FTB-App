stages:
  - buildjar
  - buildweb

build_jar:
  stage: buildjar
  trigger:
    include: subprocess/.gitlab-ci-jar.yml
    strategy: depend

build_web:
  stage: buildweb
  trigger:
    include: .gitlab-ci-front.yml
    strategy: depend
